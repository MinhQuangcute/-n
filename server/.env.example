# Copy to .env and set values
PORT=8080
# If using service account JSON file, set path below
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
# Alternatively, paste service account JSON
FIREBASE_SERVICE_ACCOUNT_JSON=
FIREBASE_DATABASE_URL=https://minhquang-36ee2-default-rtdb.firebaseio.com
# Frontend origin(s) allowed (comma-separated)
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
# Simple shared API key for additional protection (optional but recommended)
API_KEY=
